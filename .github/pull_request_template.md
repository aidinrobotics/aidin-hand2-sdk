<!-- Title: type(scope): Subject — 50 characters or fewer, imperative, no trailing period.
     The same form as a commit message header, and the same ten types.

     Delete these comments once you have written the sections. -->

## Problem

<!-- What was wrong, missing, or in the way.

     Start here rather than with the change itself. A description that opens with what was edited
     leaves the reviewer to reconstruct why it mattered, and that is the part only you know.

     Required for every type. -->

## Solution

<!-- What this change does about the problem, and what was tried and dropped along the way.

     The diff already says how, so do not walk through it. Say what the shape of the answer is
     and why it is that shape.

     Required for every type. -->

## Changelog

<!-- The CHANGELOG entry this pull request adds, under its type, written the way that file is
     written: a bold lead sentence, then the reason.

     Write NONE when nothing user-facing changes, which is most docs, style, test, refactor and
     chore work. Never leave it empty — empty does not say whether there is nothing to add or
     whether nobody wrote it. -->

```changelog

```

## Verified

<!-- Only what was actually run, so a reader can check it in seconds.

       ctest 21/21 passed on 22.04
       Built hand type a, b and c for x86_64 and aarch64
       nm -D on the built library lists no kinematics symbols

     Not "code follows the style guide" or "tested thoroughly" — neither can be checked.

     Leave this section out for docs and style changes. Fill it in for everything else. -->

## Issues

<!-- Fixes #123, or leave the section out. -->
